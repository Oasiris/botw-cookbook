import data from './all.json'

export default data

// const { materials, recipes, elixirs, dubiousFood, additiveOnlyRecipes, 
    // energizingLevels, enduringLevels, reagantDurationBonuses, 
    // priceMultipliers, effectDescriptions, effectData } = data;

// export { materials, recipes, elixirs, dubiousFood, additiveOnlyRecipes, 
    // energizingLevels, enduringLevels, reagantDurationBonuses, 
    // priceMultipliers, effectDescriptions, effectData }

const  { materials, recipes, info, elixirs } = data
export { materials, recipes, info, elixirs }